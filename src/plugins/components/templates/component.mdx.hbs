---
id: {{{key}}}
title: {{{display.label}}} Component
sidebar_label: {{{display.label}}}
description: {{{display.description}}}. Learn more about using Prismatic's {{{display.label}}} component.
---

[//]: # "DO NOT EDIT THIS FILE MANUALLY."
[//]: # "This file was auto generated using scripts in docusaurus/scripts/components/"

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import Screenshot from "@site/src/components/screenshot";
import ComponentInputsTable from "@site/src/components/component-inputs-table";
import OAuthCallbackURL from "@site/src/components/oauth-callback-url";

<div className="avatar">
  <div className="avatar__photo-link avatar__photo avatar__photo--lg">
    <img src="data:image/png;base64,{{{display.iconBase64}}}" alt="{{{display.label}}}" />
  </div>
  <div className="avatar__intro">
    <h4 className="avatar__name">{{{display.description}}}</h4>
    <small className="avatar__subtitle">
      Component key: <b>{{{key}}}</b>
    </small>
  </div>
</div>

## Description

{{{documentation.description}}}

{{{documentation.dev}}}

{{#unless (isEmpty connections)}}

## Connections

{{#each (sort connections attr="key")}}
### {{{label}}}

{{{documentation.description}}}

<ComponentInputsTable inputs={ {{{json (sort inputs attr="key")}}} } />

{{/each}}

{{/unless}}

{{#unless (isEmpty triggers)}}

## Triggers

{{#each (sort triggers attr="display.label")}}

<div id="{{{lower key}}}" />

### {{{display.label}}}

{{{display.description}}} | <small><b>key: {{{key}}}</b></small>

<ComponentInputsTable inputs={ {{{json (sort inputs attr="key")}}} } />

{{{docs}}}

{{#if examplePayload}}
#### Output Example Payload

```json
{{{json examplePayload }}}
```
{{/if}}

<hr className="componentActionSeparator" />

{{/each}}

{{/unless}}

{{#unless (isEmpty dataSources)}}

## Data Sources

{{#each (sort dataSources attr="display.label")}}

<div id="{{{lower key}}}" />

### {{{display.label}}}

{{{display.description}}} | <small><b>key: {{{key}}}</b></small> | <small><b>type: {{{dataSourceType}}}</b></small>

<ComponentInputsTable inputs={ {{{json (sort inputs attr="key") }}} } />

{{{documentation.description}}}

{{#if examplePayload}}
#### Data Source Payload

```json
{{{json examplePayload }}}
```
{{/if}}

<hr className="componentActionSeparator" />

{{/each}}

{{/unless}}

{{#unless (isEmpty actions)}}

## Actions

{{#each (sort actions attr="display.label")}}

<div id="{{{lower key}}}" />

### {{{display.label}}}

{{{display.description}}} | <small><b>key: {{{key}}}</b></small>

<ComponentInputsTable inputs={ {{{json (sort inputs attr="key") }}} } />

{{{documentation.description}}}

{{#if examplePayload}}
#### Output Example Payload
```
{{{json examplePayload}}}
```
{{/if}}

<hr className="componentActionSeparator" />

{{/each}}

{{/unless}}
